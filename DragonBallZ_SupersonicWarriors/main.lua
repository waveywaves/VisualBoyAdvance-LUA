require "overlays"

generateOverlays()


--while true do
	--local proj = tonumber("000000" .. string.sub(bit.tohex(memory.readword(0x0300682A)),7,8),16)

	--gui.text(50,50,proj)
	--emu.frameadvance()
--end
